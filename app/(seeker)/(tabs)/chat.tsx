/**
 * Seeker Chat Tab
 * Unified messaging interface for seeker communications.
 */

import React from 'react'
import ChatListScreen from '@/app/(shared)/(chat)/chat-list';

export default function chat() {
  return <ChatListScreen />;
}
