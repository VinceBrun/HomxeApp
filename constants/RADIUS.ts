type RadiusScale = {
  [x: string]: any;
  full: number;
  lg: number;
  md: number;
  sm: number;
};

const Radius: RadiusScale = {
  full: 999,
  lg: 30,
  md: 16,
  sm: 10,
};

export default Radius;
